version: '3.8'

services:
  ummah-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ummah-travel
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # - NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
    restart: unless-stopped
